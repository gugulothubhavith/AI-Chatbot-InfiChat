# ═══════════════════════════════════════════════════════════════
# InfiChat Backend Dependencies
# ═══════════════════════════════════════════════════════════════
# Install with: pip install -r requirements.txt
#
# Groups:
#   CORE      — Always required
#   STORAGE   — Database backends (pick one)
#   AI        — AI model APIs
#   FEATURES  — Optional features
# ═══════════════════════════════════════════════════════════════

# ── CORE (always required) ────────────────────────────────────
numpy==1.26.4
fastapi==0.109.2
uvicorn==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0
sqlalchemy==2.0.25
aiosqlite>=0.20.0          # SQLite async support
alembic==1.13.1
python-multipart==0.0.9
# 3.3.0 carries PYSEC-2024-233. jose is what signs and verifies every
# session token in this app, so it is not a dependency to leave behind.
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
google-auth>=2.0.0
httpx==0.26.0
geoip2>=4.8.0
stripe>=8.0.0
email-validator>=2.1.0
slowapi>=0.1.9
Pillow>=10.0.0
# 2.8.0 carries seven advisories (PYSEC-2026-120/175/177/178/179 and
# PYSEC-2025-183); 2.13.0 is the fixed line.
PyJWT==2.13.0
cryptography>=42.0.0
psutil==5.9.8
python-magic>=0.4.27
bleach>=6.1.0
tiktoken>=0.6.0

# ── STORAGE (pick your DB backend) ────────────────────────────
# For PostgreSQL (production):
psycopg2-binary==2.9.9
asyncpg==0.29.0
redis==5.0.1

# For SQLite (local dev, no Docker needed):
# SQLite is built into Python — no extra packages needed!

# ── SEARCH (pick your RAG backend) ────────────────────────────
# For ChromaDB (production, Docker):
# Pinned to the same minor as the server image in docker-compose.yml. Chroma
# changed its HTTP API between majors and the client refuses to talk to a
# server on the other side of that line; an unpinned client with a pinned
# server (or the reverse) silently degrades to the in-memory FAISS store.
chromadb==0.5.0

# For FAISS (local dev, no Docker):
faiss-cpu>=1.7.4

# Embeddings
sentence-transformers>=2.2.2

# ── AI APIs ───────────────────────────────────────────────────
groq>=0.15.0               # Groq API client

# ── DOCUMENT PARSING ──────────────────────────────────────────
pypdf>=4.0.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0
trafilatura>=1.6.0
pdfplumber>=0.10.0

# ── VOICE ─────────────────────────────────────────────────────
faster-whisper>=0.10.0
soundfile>=0.12.1
pydub>=0.25.1
edge-tts>=6.1.9

# ── RESEARCH ──────────────────────────────────────────────────
duckduckgo-search>=5.0.0
arxiv>=2.1.0
spacy>=3.7.0
yake>=0.4.8
dateparser>=1.2.0
playwright>=1.41.0
pandas>=2.0.0

# ── INFRASTRUCTURE ────────────────────────────────────────────
docker>=7.1.0              # Optional: for Docker sandbox
prometheus_client==0.20.0
pip-audit>=2.7.2
json_repair
pyotp>=2.9.0

# Web Push (VAPID) — notification delivery
pywebpush==2.0.0

# IANA timezone database for `zoneinfo`.
#
# python:3.11-slim does not guarantee /usr/share/zoneinfo, and `zoneinfo` only
# falls back to this package when the system database is absent. Without it
# ZoneInfo("Asia/Kolkata") raises, supervision_service silently falls back to
# UTC, and a parental quiet-hours window fires at the wrong time of day —
# a failure that looks like the feature working, just wrongly.
tzdata>=2024.1
